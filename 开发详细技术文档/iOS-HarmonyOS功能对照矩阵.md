# SupportClientHuawei｜iOS-HarmonyOS 功能对照矩阵

> 本文件是全项目唯一功能对照矩阵。每增加或修订一个功能详细方案，都必须同步维护对应行；不在功能文档内复制全表。

| 功能目录 | 用户能力 | iOS 代码/测试证据 | iOS 实际行为 | HarmonyOS 代码/配置证据 | HarmonyOS 目标实现 | 官方能力与 API Level | 本地示例 | 对齐状态 | 差异、风险与处理 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 文件与 OSS | STS 临时凭证、直传、远端登记、下载、账号隔离缓存、业务绑定与软删除 | `SparkClient/Projects/Core/FileStorage/{FileStorageModels,FileCacheManager,FileTransferService}.swift`、`Projects/Core/OSS/*`、`Projects/Core/Networking/API/{File,OSS}/*`、[iOS 文件与 OSS 文档](../../SparkClient/总领文档/网络、文件、OSS%20与同步基础设施/文件与OSS需求.md) | UUID/MD5/UTC object key；STS 内存快照；OSS 成功后登记；缓存按账号和 UUID 隔离 | `Core/FileStorage/*`、`Projects/Core/OSS/*`、`Projects/Core/Networking/API/{File,OSS}/*`、`entry/third_party/aliyun-oss/`、`entry/src/test/ets/FileStorage/`、[文件与OSS详细技术方案](./文件与%20OSS/文件与OSS详细技术方案.md) | 流式 download-url、`uploaded_unregistered` 恢复、`pending_delete`、generation 全覆盖、阶段进度、`cachedURL`/`publicHTTPSURLForObjectKey` | 阿里云 `@aliyun/oss` API 12+；Core File Kit；Network Kit `requestInStream`；本工程 API 24 | `HomeDecoration` `file:` 依赖；`ComprehensiveMall/.../FileUtils.ets` | 已实现+加固；P5 部分接入（聊天/药箱/AI上传报告UPLOAD） | 保持 download-url / staging / 登记补偿相对 iOS 的安全优势；AI上传报告/聊天/药箱已接 FileTransfer；完整医疗绑定与 OCR 后续待续；无自动 LRU / multipart |
| AI 上传报告：入口装配与文件 OSS 上传 | 从首页/相机/聊天进入统一上传 Host；选择图片/PDF；OSS 直传并登记为医疗上传源文件；为 OCR 阶段输出稳定附件输入 | `SparkClient/Projects/Features/MedicalDocumentUpload/*`、`AppContainer.uploadMedicalDocumentFilesUseCase`、`BindUploadedFilesToMedicalBusinessUseCase.swift`、`Core/OCR/*` | Host/Assembly/ViewModel 统一承载 upload→ocr→type_recognition→extract→attachment_binding→save；上传阶段支持跳过已上传、失败重试、断点交接 | `Projects/App/HomeTabPage.ets`、`App/AppContainer.ets`、`Projects/Features/MedicalDocumentUpload/Application/*`、`Projects/Features/Home/Presentation/MedicalLists/MedicalDocumentAttachmentUploader.ets`、[入口装配与文件OSS上传工单](./AI%20上传报告/HOS-MED-UPLOAD-0001-入口装配与文件OSS上传iOS对齐工单.md) | 新增入口参数归一化、Picker 适配器、capability 装配、checkpoint、上传错误归一化、OCR 输入边界 | ArkUI `bindContentCover`/Navigation；File Picker/Core File Kit；公共 `FileTransferService`；API 24 | 用户提供 `MedicalDocumentUploadHostView.ets`、`MedicalDocumentUploadPickingView.ets`、`MedicalDocumentUploadViewModel.ets`、`SupportingModels.ets` | 前两阶段已落地（EntryFactory/capability/Picker/Checkpoint/OCRInput）；识别闭环未接 | 本行只覆盖前两阶段；OCR/类型识别/抽取/保存进入后续工单；源文件业务类型暂用 `medical_document_upload_source`，建议中期改为 uploadSessionId |
| AI 上传报告：OCR 与文本合并 | 对图片/PDF 做 OCR，保留文件边界并合并文本，必要时做医学术语纠错，然后把合并结果交给类型识别 | `SparkClient/Projects/Core/OCR/OCROrchestrator.swift`、`OCRDocumentExtractor.swift`、`MedicalImagePreprocessor.swift`、`OCRFusionSelector.swift`、`MedicalTermsCorrector.swift`、`Projects/Features/MedicalDocumentUpload/Application/ExtractTypedMedicalDocumentUseCase.swift`、`ResolveMedicalDocumentTypeUseCase.swift` | OCR 多引擎并发；PDF 文本层优先；扫描 PDF 按页渲染；多文件文本按顺序合并；纠错后交给类型识别 | `Projects/Core/OCR/*`、`MedicalDocumentOCRUseCase.ets`、`MedicalDocumentOCRTextMerger.ets`、`ResolveMedicalDocumentTypeUseCase.ets`、`LocalMedicalDocumentTypeResolver.ets`、`MedicalDocumentUploadViewModel.ets`、[OCR 与文本合并工单](./AI%20上传报告/HOS-MED-UPLOAD-0002-OCR与文本合并iOS对齐工单.md)、[0007 偏差复核](./AI%20上传报告/HOS-MED-UPLOAD-0007-OCR与类型判定偏差复核iOS对齐工单.md) | Core Vision Kit + PDF Kit `getTextContent()` / `getCustomPagePixelMap`；未知文档缩略图；合并边界；术语纠错；checkpoint `ocr_done`/`type_resolved` | Core Vision Kit；PDF Kit；API 23/24 | Chat `HarmonyVisionOCRAdapter`；iOS OCR 目录 | 核心偏差已修（待真机验） | 真机验收文本层/扫描 PDF/未知格式；预处理仍透传；Mock OCR 默认关闭 |
| AI 上传报告：OCR 识别失败真机修复 | 用户上传截图/图片后应完成 OCR 或给出可操作错误，不能因资源释放异常直接失败 | `SparkClient/Projects/Core/OCR/OCROrchestrator.swift`、`OCRDocumentExtractor.swift` | iOS 图片解码和 Vision 任务失败不会被资源释放阶段覆盖；多引擎 `async let` 互相隔离 | `HarmonyBuiltInOCREngine.ets`、`HarmonyBuiltInOCRSession.ets`、`OCRImageSourceAdapter.ets`、`OCROrchestrator.ets`、`OCRDiagnostics.ets`、`MedicalDocumentUpload0004.test.ets`、[OCR识别失败修复工单](./AI%20上传报告/HOS-MED-UPLOAD-0004-OCR识别失败真机问题修复iOS对齐工单.md) | 包装鸿蒙通用内置 OCR：`fileIo.open(uri)` + fd 创建 ImageSource；`HarmonyBuiltInOCRSession` 管理 init/release；安全释放 ImageSource/PixelMap/textRecognition；阶段化错误；多引擎并发容错 | Core Vision Kit；Image Kit；Core File Kit；API 23/24 | 本项目 `HarmonyVisionOCREngine.ets` + 用户提供官方“用文字识别”资料 + iOS OCR 目录 | 已实现（待真机验） | release 异常不再覆盖主结果；fd 优先 + URI fallback；默认仍仅 Vision，失败有可操作文案；需真机验证 browser 截图/相册图/空白图 |
| AI 上传报告：OCR 初始化超时与预热兜底 | 打开上传页或开始 OCR 时应提前准备系统 OCR；内置 OCR 初始化超时后能重试/熔断/降级，不让用户每次等待 5 秒后失败 | `SparkClient/Projects/Core/OCR/OCROrchestrator.swift` | iOS Vision 无明显模型 init 阻塞；多引擎失败隔离，上层可重试 OCR 且不重复上传 | `HarmonyBuiltInOCRSession.ets`、`OCRWarmupCoordinator.ets`、`OCRFallbackPolicy.ets`、`OCROrchestrator.ets`、`MedicalDocumentUpload0005.test.ets`、[OCR初始化超时工单](./AI%20上传报告/HOS-MED-UPLOAD-0005-OCR初始化超时与预热兜底iOS对齐工单.md) | OCR 预热协调器；Session 状态机；init timeout reset + retry once；短 TTL keepAlive；cooldown 熔断；rootCause 优先错误提示；远端/本地 fallback 策略 | Core Vision Kit；ArkTS Promise/Timer；API 23/24 | 用户提供官方“用文字识别”资料；本项目 OCR Session | 已实现（待真机验） | 上传页打开预热；timeout 后 reset+重试一次；连续失败 cooldown 跳过 Vision；init 文案优先于“无文字”；远端 OCR 默认未开 |
| AI 上传报告：医疗文档类型判定 | OCR 后自动判断病历、体检、检查、处方、用药计划、家庭药箱，并允许手动选择覆盖 | `DefaultMedicalDocumentTypeResolver.swift`、`ResolveMedicalDocumentTypeUseCase.swift`、`MedicalPromptFactory.swift`、`MedicalDocumentTypedModels.swift`、`MedicalDocumentUploadViewModel.swift` | `manual -> localRules -> ai`；AI 返回 JSON `kind/confidence/reason`；结果写入 `typeResolution` 并驱动后续抽取类型 | `DefaultMedicalDocumentTypeResolver.ets`、`MedicalDocumentTypeAIClient.ets`、`MedicalDocumentTypeResponseDecoder.ets`、`LocalMedicalDocumentTypeResolver.ets`、`ResolveMedicalDocumentTypeUseCase.ets`、`MedicalDocumentUpload0007.test.ets`、[医疗文档类型判定工单](./AI%20上传报告/HOS-MED-UPLOAD-0003-医疗文档类型判定iOS对齐工单.md)、[0007 偏差复核](./AI%20上传报告/HOS-MED-UPLOAD-0007-OCR与类型判定偏差复核iOS对齐工单.md) | Default resolver + AI Client/Decoder；弱命中 reason=`advanced_keyword_rules`；fallback confidence=0.4；低置信暂停 + 确认/改类型 UI | 复用 `Core/AI/Runtime` 与账号级 AI 配置；API 24 | PromptLocalizer / StructuredRuntimeClient | 核心偏差已修（低置信闸门按工单实现） | AI 依赖账号 bootstrap；HOS 低置信确认强于 iOS 生产现状；抽取仍见 0006 |
| AI 上传报告：结构化草稿数据模型 | 六类 RecognitionDraft 树、ItemDraft/MedicalReportItem、处方规范化、草稿→保存请求映射 | `MedicalDocumentTypedModels.swift`、`MedicalDocumentDraftMappers.swift`、`PrescriptionFieldNormalization.swift` | `typedResult` 枚举关联草稿 + normalize + toCreateRequest | `MedicalDocumentRecognitionDrafts.ets`、`MedicalDocumentDraftBuilders.ets`、`MedicalDocumentDraftMappers.ets`、`PrescriptionFieldNormalization.ets`、`MedicalDocumentTypedResultMapper.ets`、`MedicalDocumentUpload0008.test.ets`、[0008 工单](./AI%20上传报告/HOS-MED-UPLOAD-0008-结构化草稿数据模型字段级对齐iOS工单.md) | 分类型草稿树 + JSON 构建 + 状态/频次归一 + 保存 DTO 映射 | ArkTS Domain；API 24 | iOS Domain | 已实现（待联调验收） | 结果编辑页 ResultPages、远端保存接线、PrescriptionRecognitionDraftMapper 互转仍待后续 |
| AI 上传报告：预提交校验与断点续跑 | 保存前字段级校验阻断；失败后按阶段恢复，不重复上传/OCR/类型/抽取 | `MedicalPreSubmitValidator.swift`、`MedicalPreSubmitValidationIssue.swift`、`MedicalDocumentUploadViewModel.swift` | save 前 validate；issue 驱动滚动/折叠；识别态可续跑 | `MedicalPreSubmitValidator.ets`、`MedicalPreSubmitValidationRules.ets`、`MedicalPreSubmitValidationIssue.ets`、`MedicalDocumentUploadCheckpointStore.ets`、`MedicalDocumentUploadViewModel.ets`、`MedicalDocumentTypedResultPage.ets`、`MedicalDocumentUpload0008b.test.ets`、[0008 工单](./AI%20上传报告/HOS-MED-UPLOAD-0008-预提交校验与断点续跑iOS对齐工单.md) | 独立校验层 + Preferences 非敏感 OCR/type/extract 快照 + 步骤 SKIPPED + attemptSave 门 | Preferences；ArkTS；API 24 | iOS Domain/VM | 已实现（待联调验收） | 正式 SaveUseCase 未接线；结果页字段级自动滚动留给结果页工单；禁止 Preferences 存 STS/Token |
| AI 上传报告：附件与业务结果匹配 | 抽取后把源文件匹配到 typedDrafts；保存载荷带 file_ids；保存后回写 businessType | `MedicalDocumentAttachmentBusinessMatcher.swift`、`DefaultMedicalDocumentAttachmentBinder.swift`、`BindUploadedFilesToMedicalBusinessUseCase.swift` | 本地 UUID→attachmentFileIds；save 嵌入 file_ids；PATCH files/business/update | `MedicalDocumentAttachmentBusinessMatcher.ets`、`MedicalDocumentAttachmentBindingMapper.ets`、`DefaultMedicalDocumentAttachmentBinder.ets`、`BindUploadedFilesToMedicalBusinessUseCase.ets`、`MedicalAttachmentBindingBridge.ets`、`MedicalDocumentUpload0010.test.ets`、[0007 工单](./AI%20上传报告/HOS-MED-UPLOAD-0007-附件与业务结果匹配iOS对齐工单.md)、[0010 补充](./AI%20上传报告/HOS-MED-UPLOAD-0010-0007偏差复核补充iOS对齐工单.md) | ATTACHMENT_BINDING 本地匹配 + file_ids 映射 + binder 装配；复用 typedDrafts | FileAPI PATCH；Workflow file_ids；API 24 | iOS Domain | 已实现匹配/映射（待保存联调） | SaveUseCase 未接线；服务端 bind 待保存后调用；OCR 0007 测试文件勿覆盖 |
| AI 上传报告：各类型结果页 | 按 kind 分流到病例/体检/检查/处方/用药计划/药箱确认壳；可编辑草稿、附件回显、预提交问题 | `MedicalDocumentResultRouterView.swift`、`Presentation/ResultPages/*` | typedResult switch → 六类 ResultView；Banner + scroll/collapse | `MedicalDocumentResultRouterView.ets`、`ResultPages/*View/*ContentView/*Support`、`MedicalDocumentTypedResultScaffoldView.ets`、`MedicalDocumentUpload0008c.test.ets`、`MedicalDocumentUpload0011.test.ets`、`MedicalDocumentUpload0012.test.ets`、[0008 工单](./AI%20上传报告/HOS-MED-UPLOAD-0008-各类型结果页iOS对齐工单.md)、[0011](./AI%20上传报告/HOS-MED-UPLOAD-0011-各类型结果页实现偏差与优化方向iOS对齐工单.md)、[0012](./AI%20上传报告/HOS-MED-UPLOAD-0012-各类型结果页目录文件组件完全一致补充工单.md) | Host→RouterView→六类 View/ContentView；共享 scaffold；PreSubmit Navigation/Banner；Typed 页兜底 | ArkUI Scroll/ForEach/@BuilderParam；API 24 | iOS ResultPages | 已实现（待联调验收） | 病例 visit/治疗占位；药箱远端候选匹配占位；保存后详情桥接 stub |
| AI 上传报告：结果页结构与 iOS 同构 | View/ContentView/Support/Sections 拆分；共享 scaffold 消除壳层重复 | `ResultPages/*View.swift`、`*ContentView.swift`、`*Support.swift`、`*Sections.swift` | 每类型至少 View+Content+Support+Sections；PreSubmitValidation 独立目录 | `MedicalDocumentResultSupport.ets`、`MedicalDocumentTypedResultScaffoldView.ets`、`PreSubmitValidation/*`、`CaseRecognitionResultView.ets` 等、`MedicalDocumentUpload0011.test.ets`、`MedicalDocumentUpload0012.test.ets`、[0012 工单](./AI%20上传报告/HOS-MED-UPLOAD-0012-各类型结果页目录文件组件完全一致补充工单.md) | 0012 目录/文件/组件命名与 iOS 对齐；旧 `*Page.ets` 单体已拆除 | ArkTS 组件拆分；API 24 | iOS ResultPages | 已实现（待联调验收） | Case 分区文件部分合并为 Sections.ets；深度本地 editor sheet 仍待增强 |
| AI 上传报告：结果确认与保存 | 预提交通过后按类型落库；file_ids 写入保存载荷；成功回执、dismiss、列表刷新 | `SaveTypedMedicalDocumentUseCase.swift`、`DefaultTypedMedicalDocumentSaver.swift` | 六类 API + receipt；payload 内 file_ids；VM saveResult | `SaveTypedMedicalDocumentUseCase.ets`、`DefaultTypedMedicalDocumentSaver.ets`、`CombinedMedicalCreateAPI.ets`、`MedicalWorkflowAPI.ets`、`MedicalDocumentUploadViewModel.ets`、`MedicalDocumentUpload0009.test.ets`、[0009 工单](./AI%20上传报告/HOS-MED-UPLOAD-0009-结果确认与保存iOS对齐工单.md) | Saver 六类编排；attemptSave→saveResult；bind best-effort；capabilities.save 由 Assembly 开启 | HTTP workflow/combined-create；API 24 | iOS Saver | 已实现（待联调验收） | 首页监听 `saveSucceededRevision` 待接；处方 preflight 专用错误可继续增强；绑定失败仅日志 |
| 网络模块 | 统一请求、Token、401、ETag、重试和缓存 | `SupportClient/Core/Networking/`、`SupportClientTests/Networking/` | 由组合根统一装配，页面不直接发请求 | `App/AppContainer.ets`、`Core/Networking/SupportBackend.ets`、`Projects/Core/Networking/*`、`Foundation/Security/SecureTokenStore.ets`、`entry/src/test/ets/Networking/NetworkModule.test.ets`、`module.json5` INTERNET | 与 iOS 同契约的 Engine/Transport/Token/401/ETag/Retry/串行/GET 合并 | `http-request`、Network Kit、ArkData、API 24 | LiveStreaming utils、News commons/network | 部分对齐 | ETag 已对标（真 SHA-256、过期删除、`X-Cache-Max-Age`、医疗 list 120s）；鸿蒙按 accountScope 清 ETag（优于 iOS 登出保留）；SecureTokenStore 可再强化 HUKS |
| 键值存储与跨设备同步 | iOS UserDefaults 轻量偏好、账号 scope、运行时状态镜像 | `SparkClient/总领文档/基础工程与运行环境/`、AI 设置文档 | HarmonyOS 公共 `ProjectKeyValueStore`；本地 KV + 设备协同 KV；AI 偏好通过 Adapter 接入 | 当前 `DeviceCache.ets`、`LegalAcceptanceStore.ets` 使用 Preferences；目标 `Core/Storage/ProjectKeyValueStore.ets`、`ProjectKVManager.ets` | `@kit.ArkData distributedKVStore`、SINGLE_VERSION、DEVICE_COLLABORATION、dataChange | 部分对齐 | Preferences 已有局部实现；公共 KV 门面、跨设备双机验证、同步冲突处理待实现 |
| 日志系统 | 统一输出、脱敏、Request ID、启动/网络诊断 | `SupportClient/Foundation/Logger.swift`、`总领文档/.../日志系统需求.md` | SupportLogger 等级/模块/截断/脱敏 | `Foundation/Logging/*.ets`、`AppContainer` 配置、`EntryAbility` StartupLogger、`entry/src/test/ets/Logging/LoggingSystem.test.ets` | hilog 统一 Logger + LogSanitizer + Startup/Network/Error 门面 | `ohos.hilog`、hdc，API 24 | MoneyTrack Logger 仅形态 | 部分对齐 | 本体已实现；无文件/远端 sink；accountHash 待会话模块 |
| 资源与多语言 | 应用名、权限说明、页面文案、启动图标和语言切换 | `SupportClient/总领文档/基础工程与运行环境/资源与多语言需求.md`、`Projects/App/Resources/en.lproj/Localizable.strings`、`zh-Hans.lproj/Localizable.strings`、`InfoPlist.strings` | UI 文案、权限说明和应用信息按语言目录拆分，页面硬编码被资源化替代 | `AppScope/resources/{base,zh_CN,en_US}/element/string.json`、`entry/.../{base,zh_CN,en_US}/element/string.json`、`Foundation/L10n/L10n.ets`、`pages/Index.ets`、`entry/src/test/ets/Resources/ResourcesI18n.test.ets` | 跟随系统语言的 `$r` 资源与 L10n | `Resource`、`ohos.i18n`，API 24 | News/Community 模板 locale 目录 | 部分对齐 | 本期最小集已落地；AI 种子与第二相机专用表待功能模块；无应用内切语言 |
| 路由与页面导航 | 启动页、登录页、主框架 Tab、账号页、第二相机、系统失效回退 | `SupportClient/Projects/App/Sources/App/AppCoordinatorView.swift`、`RouteCoordinator.swift`、`LoginView.swift`、`PhoneLoginView.swift`、`SettingsView.swift`、`SecondCameraHomeView.swift`、`NotificationHostView.swift` | 状态驱动导航，局部 `NavigationStack` / `sheet` / `fullScreenCover` | `App/AppSessionStore.ets`、`RouteCoordinator.ets`、`NotificationHost.ets`、`pages/Index.ets`、`Projects/App/MainTabsPage.ets`、`Projects/Features/Auth/LoginPage.ets`、`Projects/Features/Settings/SettingsPage.ets`、`entry/src/test/ets/Routing/RoutingShell.test.ets` | 已实现路由壳：根会话态、设备登录、三 Tab、设置二级/升级 sheet、失效回登录；OTP/相机/深链为占位或预留 | `UIAbility`、`Navigation`、`Tabs`、`NavPathStack`、`bindSheet`，API 24 | `HouseAndHomeTemplate/.../MainEntry.ets`、`GovernmentService/.../LoginSheetUtils.ets` | 部分对齐 | 完整 OTP/Apple/真实相机/深链与账号准备门禁待后续；冷启动恢复已接领域底座 |
| 应用内通知 | toast、banner、alert、通知队列去重、收件箱、指标、业务反馈入口 | `SupportClient/Projects/App/Sources/ContentView.swift`、`App/Notification/NotificationHostView.swift`、`NotificationFullScreenCover.swift`、`Projects/Core/Notification/{Domain,Application,Infrastructure}`、`总领文档/应用内通知/应用内通知需求.md` | 业务通过 `NotificationClient` 发布；toast/banner/alert 串行展示；2 秒去重；队列最大 128；inbox 最多 500 条；metrics 记录展示和丢弃；banner 支持点击 | `Projects/Core/Notification/**`、`App/Notification/*`、`AppContainer.ets`、`pages/Index.ets`、`LoginViewModel.ets`、`entry/src/test/ets/Notification/*`、应用内通知方案 | P1–P5 已落地；P6 Push 不做 | ArkUI `AppStorage`、`promptAction.showDialog`、Core File Kit `file.fs` API 24 | `aggregated_login` toast、`DialogController.ets`、`SessionSnapshotStore.ets` | 部分对齐 | 无系统 Push/Dashboard UI；inbox 登出/切换清空；共用根 Host |
| 应用启动链路 | 冷启动、网络门控、会话恢复、游客/已登录设备登记、账号级准备、主框架放行、鉴权失效中止 | `SupportClient/.../AppLifecycleCoordinator.swift`、`AppBootstrapper.swift`、`DeviceRegistrationCoordinator.swift`、`SignedInSessionPreparationRegistry.swift`、`总领文档/应用启动链路/` | 网络评估后恢复；游客登记；已登录设备登记+AI/OSS，`preparedAccountID` 后进主 Tab；401 中止 | `AppLifecycleCoordinator.ets`、`AppBootstrapper.ets`、`DeviceRegistrationCoordinator.ets`、`DeviceAPI.ets`、`SupportSystemInfo.ets`、`SignedInSessionPreparationRegistry.ets`、`NetworkGateView.ets`、`pages/Index.ets`、`entry/src/test/ets/Startup/*`、启动链路/设备登记方案 | 启动 P1–P5 + 设备登记 P3–P6；OSS 待后续 | UIAbility、Network Kit、HUKS、Navigation/Tabs，API 24 | RentAndBuy EntryAbility/Index、LiveStreaming NetConnectionUtil | 部分对齐 | OSS STS skip；设备凭证 HUKS 已接（失败回退）；SecureToken HUKS 仍待 |
| 应用启动链路 | 设备登记：匿名/已登录上报终端信息，后台可信设备画像与吊销 | 后端 `SparkService/accounts/...`；iOS `DeviceRegistrationCoordinator.swift`、`SupportAPIs.swift`、`设备登记需求.md` | `POST /api/v1/device/register/`；匿名/已登录；3 次退避；401 中止；`TrustedDevice` | `DeviceRegistrationCoordinator.ets`、`DeviceAPI.ets`、`SupportSystemInfo.ets`、`DeviceCredentialStore.ets`、`HuksAesGcmCipher.ets`、`DeviceRegistrationSubmittedSnapshot.ets`、`DeviceCache.ets`、`DeviceRegistration.test.ets`、设备登记方案 | P0–P6 已落地；P7 联调未做 | HTTP、HUKS、Core File Kit、Preferences，API 24 | HomeDecoration network、LiveStreaming NetConnectionUtil | 部分对齐 | Push 未接；后台 iOS 文案属 backoffice；真机联调待 P7 |
| 会话与认证 | Apple/手机号验证码/设备游客登录、冷启动恢复、Token 刷新、退出登录、会话失效清理 | `SupportClient/Projects/Features/Auth/`、`AppSessionStore.swift`、`SparkKeychain.swift`、`AuthTokenProvider.swift`、`SparkNetworkEngine.swift`、`SupportAuthSessionPersistenceTests.swift` | 三种登录方式进入同一 `UserSession`；登录与刷新使用相同安装级 `device_id` 和 `bundle_id`；刷新暂不可用保留会话，明确失效清理；登出本地必清 | `UserSession.ets`、`SessionSnapshotStore.ets`、`DeviceCredentialStore.ets`、`SparkAssetStore.ets`、`DefaultAuthRepository.ets`、`AppSessionStore.ets`、`AuthAPI.ets`、`OTPAPI.ets`、`AuthTokenProvider.ets`、`SessionInvalidation.ets`、`entry/src/test/ets/Networking/NetworkModule.test.ets` | 登录/启动恢复/刷新/失效底座部分对齐；刷新默认 `bundle_id` 已从遗留 SupportClient 值收敛到 `AUTH_BUNDLE_ID`；华为账号占位 | `http-request`、Asset Store、沙箱加密、Navigation/Tabs/ArkWeb，API 24 | `aggregated_login`、LiveStreaming Token 拦截器 | 部分对齐 | 已修复 Refresh `bundle_id` 与登录/App bundle 不一致导致的设备会话 401；403 不退出、临时刷新失败不升级退出；仍需服务端真机联调及 Account Kit 换票；不做 Apple 入口 |
| 会话与认证 UI 设计图 | 登录首页、协议半屏、手机号输入、地区选择、6 位验证码、协议阅读器 | `SupportClient/总领文档/会话与认证/会话与认证UI设计图.md`、`LoginView.swift`、`LoginConductor.swift`、`VerificationCodeField.swift`、`LoginLegalAgreementNote.swift`、`PhoneRegions.swift` | 截图级 UI 已覆盖 Apple 登录、手机号登录、游客模式、协议门禁、地区菜单、OTP 倒计时和 Safari 协议阅读 | `LoginPage.ets`、`PhoneLoginPage.ets`、`OtpVerifyPage.ets`、`LoginViewModel.ets`、`AuthUiState.ets`、`PhoneRegion.ets`、`components/*`、`LegalAcceptanceStore.ets`、`entry/src/test/ets/Auth/AuthUi.test.ets`、`开发详细技术文档/会话与认证/会话与认证UI设计图详细技术方案.md` | P0–P7 已落地：门禁半屏、发码/验码 UseCase、ArkWeb 占位 URL、华为「即将支持」 | ArkUI `Navigation`、`TextInput`、`Button`、`Checkbox`、`bindSheet`、ArkWeb `Web`，API 24 | `aggregated_login` | 部分对齐 | 华为换票未接；无地区旗帜；OTP 不自动提交；协议 URL 为占位 HTTPS |
| 设置与账户管理 | 设置页账户入口、游客升级分流、账号摘要、退出登录 | `SettingsView.swift`、`AppCoordinatorView.swift`、`SignOutUseCase.swift`、`总领文档/设置与账户管理/账户管理需求.md` | 正式账号点击进入账户管理，游客账号打开升级登录；退出登录走 `SignOutUseCase` 并发布 signedOut | `SettingsPage.ets`、`SettingsAccountRouting.ets`、`AccountManagementPage.ets`、`LoginPage.ets`、`AppContainer.signOut`、`AppSessionStore.ets`、`开发详细技术文档/设置与账户管理/账户管理详细技术方案.md` | 正式/游客分流、摘要、升级合并；退出确认在账户页 | Navigation、bindSheet、Dialog，API 24 | `aggregated_login` LoginSheetUtils | 已对齐 | 退出失败仍尽力本地 signedOut |
| 设置与账户管理 | 账号资料、身份列表、手机号/邮箱/Huawei 绑定换绑、账号注销 | `Projects/Features/AccountManagement/`、`AccountIdentityAPI.swift`、`DeactivationAPI.swift`、`OTPAPI.swift`、`总领文档/设置与账户管理/账户管理需求.md` | 并发加载资料和身份列表；绑定/换绑先再验证再提交目标；注销先验证再最终确认，成功后本地登出 | `Projects/Features/AccountManagement/{Domain,Application,Infrastructure,Presentation}/`、typed `AccountIdentityAPI.ets`/`DeactivationAPI.ets`、`entry/src/test/ets/AccountManagement/`、技术方案 | P1–P6 已落地；Huawei ComingSoon | HTTP、HUKS、取消请求、Navigation，API 24 | LiveStreaming network/interceptor、aggregated_login | 已对齐（phone/email） | Account Kit / P7 联调未做；邮箱注销 scene=`login`（R5） |
| AI 配置生命周期与本地、Pro 统一消费 | 本地配置、Pro bootstrap、试用、Provider 测试、本地/Pro 合并和统一 AI 场景消费 | `SparkClient/Projects/Core/Networking/API/AI/AIConfigAPI.swift`、`SparkClient/Projects/Core/AI/`、`SparkClient/总领文档/AI 设置与本地模型/AI 配置生命周期与本地、Pro 统一消费.md` | 本地快照优先；远端 revision/ETag 刷新；按 19 个场景解析 `AIResolvedConfig`；运行时统一消费 | `entry/src/main/ets/Core/AI/`、`entry/src/main/ets/Core/Storage/`、`entry/src/main/ets/Projects/Core/Networking/API/AI/AIConfigAPI.ets`、`entry/src/main/ets/App/AppBootstrapper.ets` | ArkData RDB、HUKS secretRef、SparkService bootstrap、Resolver、Pro/Local Adapter | 部分对齐 | RDB、本地/Pro 合并、统一 Runtime 和启动接入已有；ETag 条件刷新、事务恢复、生产安全和真实联调待完善 |
| AI 设置场景配置（厂商key、模型、默认模型配置、小任务） | Provider Key、模型/Agent 目录、默认模型、场景绑定、小任务、截图对应 UI 和保存/错误/账号隔离行为 | iOS `SparkClient/Projects/Features/AISettings/`；HarmonyOS 方案 `开发详细技术文档/AI 设置与本地模型/AI 设置场景配置（厂商key、模型、默认模型配置、小任务）详细技术方案.md` | HarmonyOS `Projects/Features/AISettings/`、`Core/AI/`、`Core/Storage/`、RDB/HUKS、ArkUI 页面 | ArkData RDB、Universal Keystore Kit、Core File Kit、Navigation、HTTP | `ShoppingTemplate/Express` RDB、`HomeDecoration` network、`Community/ChatInput` Preferences | 部分对齐 | HarmonyOS 已有本地快照、seed、HUKS、Resolver、Runtime 和聚合设置页；完整子页面、GGUF 下载/推理、HUKS 失败安全收口、事务/冲突/真实联调待完善 |
| 设置与账户管理 UI 设计图 | 账户资料卡、身份行、再验证浮层、OTP、注销确认、错误/空态 | `AccountManagementView.swift`、`AccountManagementComponents.swift`、`AccountManagementViewModel.swift`、`账户管理需求.md` | SwiftUI 使用滚动主页面 + 卡片区块 + overlay 承载高风险流程 | `账户管理UI设计图.md`、`AccountManagementPage.ets`、`components/*`、`account_*` 资源键 | 主页五区块 + 身份/注销浮层已落地 | Navigation、bindSheet、PromptAction、CustomDialog，API 24 | aggregated_login、LiveStreaming network | 已对齐 | 注销进度/撤销 UI 不做（UI-R4） |
| 首页、成员与医疗画像 | 已登录首页双段式加载：成员列表确定当前成员，再拉 complete-data 生成 4 类医疗卡片并缓存快照供列表首屏 | iOS `LoadHomeMedicalOverviewUseCase.swift`、`HomeViewModel.swift`、`HomeDashboard.swift`、`MemberContextStore.swift`、`MedicalQueryAPI.fetchMemberCompleteData`；后端 `MemberCompleteDataAPI`；[首页医疗接口](../../SparkClient/总领文档/首页、成员与医疗画像/首页医疗接口.md) | `listMembers` 失败抛错；complete-data 失败降级空卡片；选中优先级 传入>持久化>首位；同 member 保留 `familyMedicineBoxes`；模块配置二次请求控制分区 | `API/Medical` 12 文件 + Home/MemberContext + ETag 策略对标（list 120s / complete-data 86400） | `MedicalQueryAPI` + UseCase + HomeVM + MemberContext + Preferences 选中成员；见 `开发详细技术文档/首页、成员与医疗画像/首页医疗接口详细技术方案.md` | HTTP/ETag via 网络模块、Preferences、Navigation，API 24 | Parking `HomePageVM`；工程内 Network Engine | 部分对齐 | 首屏 listMembers/complete-data/模块配置/四卡/成员切换已通；ETag TTL/serialKey/X-Cache-Max-Age 已对标；写接口与列表注入待补；成员列表路径为 `GET /api/v1/medical/resources/?kind=members`；admin complete-data 不可作客户端契约 |
| 首页、成员与医疗画像 | 医疗 Networking API 目录与 iOS `API/Medical/` 文件名 1:1 | iOS 12 文件：`CombinedMedicalCreateAPI`…`SparkMedicalResourceKind` | 同路径语义、同文件名 | `entry/.../API/Medical/*.ets` 已建齐 12 文件 | 继续补非首页方法 | ArkTS，API 24 | — | 部分对齐 | 禁止 `MedicalSyncModels` 等另起名；`+Invite`/`+Share` 保留文件名 |
| 首页、成员与医疗画像 | 医疗数据模型与 iOS `API/Medical` 十二文件一致（Remote* / SavePayload / Invite-Share / Combined / ExamArchive / complete-data） | iOS 十二文件 + `ReminderTime.swift` + `HomeDashboard.swift` | snake_case 默认；`UpsertMemberPayload` camelCase；ExamArchive/Invite/BundleItem 显式 snake_case | complete-data 首页相关 Remote* 已落地；其余按 §5.10 继续 | 按方案 §5.3–5.6 + **§5.10** 补全 SavePayload/Invite 等 | ArkTS 严格类型，API 24 | — | 部分对齐 | 禁止另起字段名/文件名；编码例外须单测 |
| 首页、成员与医疗画像 | 医疗列表首屏复用 complete-data；下拉仅刷新当前资源并 patch 首页缓存；同成员保留 `familyMedicineBoxes`；Chat 镜像须同 `memberId` | iOS `HomeViewModel.updateMedicalCompleteData`、`HomeMedicalRouteSupport`、`MedicalLists/**`、`FamilyMedicineCabinetPage`、`ChatDetailViewModel.cachedMemberCompleteData`；[首页医疗数据缓存](../../SparkClient/总领文档/首页、成员与医疗画像/首页医疗数据缓存.md) | 权威源=`dashboard.medical.completeData`；字段级回写；归档不回写活跃；无 Disk 持久化健康快照；patch 不重算 cards（已知缺口） | `updateMedicalCompleteData`/`reloadMembers`/`HomeMedicalRouteSupport`/`HomeMedicalListPage`/`FamilyMedicineCabinetPage` + QueryAPI list* 已落地；Chat 镜像待 Chat Feature | 见 `开发详细技术文档/首页、成员与医疗画像/首页医疗数据缓存详细技术方案.md` | 内存对象 + Navigation 注入；禁止 Preferences 存 complete-data；API 24 | Parking `HomePageVM`；HomeDecoration Navigation | 部分对齐 | 列表壳与缓存闭环已通；明细/表单/OCR/Chat 镜像待补；已知缺口（cards 不同步）须显式记录 |
| 首页、成员与医疗画像 | 成员切换与选中持久化、member-module-settings、首页医疗/饮食/维护分区、公共添加/维护 SetupFlow | iOS `HomeView`/`HomeViewModel`/`MemberModuleSetupUseCase`/`Members/MemberSetup/**`/`SelectedMemberIDPersisting`；后端 `MemberModuleSetting`+unified `kind=member-module-settings`；[首页成员模块](../../SparkClient/总领文档/首页、成员与医疗画像/首页成员模块.md) | 默认不开通只看 isEnabled；选中账号隔离；create 默认开通 medical；finish 不关模块；settings 失败无 toast | `SelectedMemberIDStore`+Context+list/upsert settings+分区 getter（**default-off**）+`MemberSetupFlow` create/maintain + HomeTab `+`/维护；饮食区 UI / MemberDetail / 汇总子流待补 | 见 `开发详细技术文档/首页、成员与医疗画像/首页成员模块详细技术方案.md` | Preferences + HTTP/ETag + Navigation，API 24 | LegalAcceptanceStore；Parking HomePageVM；aggregated_login sheet | 部分对齐 | **空 settings default-off 已对齐 iOS**；SetupFlow 主路径已通；饮食首页区与汇总子流待补 |
| 首页、成员与医疗画像 | 体检报告：列表缓存首屏、明细懒加载、详情删档分享、上传 `health-exams/save` | iOS `HealthExamReportsListPage`/`ExamReportCard`/`MedExamDetailLazyLoadViewModel`/`HealthExamRecognitionResultView`/`DefaultTypedMedicalDocumentSaver.saveHealthExam`；后端 `HealthExamReportViewSet`/`MedExamDetailViewSet`/`HealthExamWorkflowSaveView`；[体检报告](../../SparkClient/总领文档/首页、成员与医疗画像/体检报告.md) | kind=`health-exam-reports`；`business_type=health_exam_report`；活跃 patch；归档切断；OCR source=2 status=1 | `HealthExamReportsListPage`/`ExamReportCard`/`HealthExamReportDetailPage`/`HealthExamDetailLazyLoadController`；Query `listMedExamDetails`；Workflow save/delete/archive；DTO 含 `medExamDetails`；上传 OCR/分享/Chat 待 | 见 `开发详细技术文档/首页、成员与医疗画像/医疗检查报告/体检报告详细技术方案.md` | Navigation/NavDestination、HTTP via Engine、Camera/Photo 待权限；API 24 | HomeDecoration `ListPage`/`module_filter_list`；工程内 Network Engine | 部分对齐 | P6 接 OCR/文件；分享；禁止 admin complete-data；列表异常启发式与详情 riskLevel 两套规则需显式对齐 |
| 首页、成员与医疗画像 | 医疗检查报告：检验/影像/病理列表缓存首屏、明细懒加载、三类卡片与详情、删档分享、上传 `medical-reports` | iOS `ExaminationReportsListPage`/`LabReportCard`/`MedExamDetailLazyLoadViewModel`/`Laboratory|Imaging|Pathology` 详情；后端 `ExaminationReportViewSet`/`MedExamDetailViewSet`；[医疗检查报告](../../SparkClient/总领文档/首页、成员与医疗画像/医疗检查报告.md) §二点五 | kind=`examination-reports`；`business_type=examination_report`；活跃 patch；归档切断；与体检共用 `med-exam-details` | 独立 `ExaminationReports/` 列表+`LabReportCard`+总览详情+懒加载+删档；DTO 含 `medExamDetails`；`setExaminationArchived`；OCR/三类深度细页/分享待 | 见 `开发详细技术文档/首页、成员与医疗画像/医疗检查报告/医疗检查报告详细技术方案.md` | Navigation/NavDestination、HTTP via Engine；API 24 | HomeDecoration `ListPage`；Parking `HomePageVM`；工程内 Network Engine | 部分对齐 | 列表/卡片/详情/懒加载/删档已通；OCR/手工录入占位；Lab/Imaging/Pathology 独立细页与分享待；禁止与体检 kind 混用 |
| 对话、AI Runtime 与工具调用 | 统一推理入口：场景选模、本地/远端路由、tools 降级、流式事件、取消；聊天编排多轮 tool；Chat 真实入口与消息落库 | iOS `Core/AI/` + `Core/AIRuntime/` + `Features/Chat/`、[AI Runtime 推理编排需求](../../SparkClient/总领文档/对话、AI%20Runtime%20与工具调用/AI Runtime 推理编排需求.md) | 真 SSE；Orchestrator≤30 轮；Agent baseModel；空输出；Feature 直连同一 `generateTextStream` | `Core/AI/Runtime/*` + `Domain/{AIRuntimeErrors,AIRuntimeMappers,AIRuntimeSseParser,OpenAIChatBodyEncoder}` + `ScenarioPolicyResolver`；`Features/Chat`（RDB/Repository/SendUseCase/MessageRunActor/ChatTabPage/ChatSyncEngine/ChatAttachmentPipeline）；`ToolHub/`+`debug_echo`；主 Tab「对话」 | ToolHub 业务工具/授权/审计；医疗营养非 chat 消费者；GGUF；真机 SSE 长流 | Network Kit `requestInStream`，API 24；HUKS Key | FileCacheManager 流式生命周期 | 部分对齐（已落地 Chat 入口、消息持久化、REST/WS 同步、线程软删除、消息重试、附件下载任务消费） | 正式工具业务与非 chat Feature、附件选择/OCR、真机 SSE/WS/OSS 验收仍待；健康工具生产默认明确不可用 |

## 状态定义

| 状态 | 使用条件 |
| --- | --- |
| 已验证对齐 | 两端代码/测试证明相同契约和可观察行为已实现。 |
| 部分对齐 | 已有目标端实现，但字段、失败分支、权限、缓存、并发或验收仍有缺口。 |
| 待实现 | 参考端能力已确认，目标端没有对应实现或仅有空壳。 |
| 不适用 | 能力确实没有目标端业务需求或受平台限制不能提供；必须说明替代产品行为。 |

## 维护规则

1. 只引用真实路径、可访问官方 URL 和已核验本地示例。
2. “已验证对齐”必须有测试、运行证据或逐字段代码对比；不能因接口名相同就标绿。
3. 每行只描述一个可验收功能；复杂能力拆为启动、认证、缓存、上传等多行。
4. 对新学习到且可跨项目复用的 HarmonyOS 对照，同步追加到 Skill 的知识库；仅项目特有内容只留在本矩阵。
