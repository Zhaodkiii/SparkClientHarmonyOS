/** 药箱识别结果页内栈路由。对齐 iOS `MainNavigationLink` 推入本地草稿详情。 */
export class MedicineBoxRecognitionResultRoutes {
  static readonly LOCAL_DRAFT_DETAIL: string = 'medical.upload.result.medicine_box.local_draft_detail';
}

/** 药箱识别结果页卡片 → 本地草稿详情导航目标。 */
export class MedicineBoxCardDetailTarget {
  index: number = 0;
}
